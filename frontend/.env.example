# FastAPI base URL (no trailing slash). Default in code: http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000

# Set to "true" only during development to preview the gait-phase timeline UI with mock data.
# VITE_DEV_ML_MOCK=true
